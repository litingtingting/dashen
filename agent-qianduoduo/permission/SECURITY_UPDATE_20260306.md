# 安全更新公告 - 2026-03-06

**主题**: 敏感信息零明文输出（v2.0 最终版）  
**优先级**: 🔴 最高  
**生效日期**: 立即生效  
**版本**: v2.0 (最终版)

---

## 📢 核心原则（不可逾越的红线）

**任何敏感信息（密钥/token/密码/配置等）禁止在任何对话、日志、输出中明文显示！**

**包括管理员！包括任何渠道！包括任何情况！**

---

## 🔐 为什么？

| 风险 | 说明 |
|------|------|
| **渠道伪造** | webchat/cli 被认为安全，但会话可能被劫持 |
| **日志泄露** | 对话历史、日志文件可能被未授权访问 |
| **中间人攻击** | 网络传输可能被截获 |
| **内部威胁** | 管理员账户可能被盗用 |
| **合规要求** | 安全最佳实践要求最小化敏感信息暴露 |

---

## 📋 权限分级矩阵（v2.0）

| 问题类型 | ADMIN | COLLEAGUE | PUBLIC |
|---------|-------|-----------|--------|
| API 密钥/Token | ❌ **显示哈希** | ❌ 仅状态 | ❌ 仅状态 |
| 账户密码 | ❌ **不显示** | ❌ 拒绝 | ❌ 拒绝 |
| 配置文件 | ❌ **脱敏显示** | ❌ 拒绝 | ❌ 拒绝 |
| 持仓/仓位 | ⚠️ 脱敏显示 | ⚠️ 脱敏 | ❌ 拒绝 |
| 交易指令 | ⚠️ 脱敏显示 | ⚠️ 脱敏 | ❌ 拒绝 |

**关键**: ADMIN 渠道可以执行敏感操作，但**敏感信息本身不显示明文**。

---

## 🛠️ 正确输出示例

### 管理员问 Token
```
丽斯（webchat）: ITICK_TOKEN 是多少？

钱多多：
抱歉，出于安全考虑，敏感信息不能明文显示。
当前状态：已配置 (SHA256: aeab308b...)

如需验证 Token 是否正确，可以告诉我一个操作，
我来帮您执行（如"获取实时行情"）。
```

### 输出格式
```
✅ ITICK_TOKEN: 已配置 (SHA256: aeab308b...)
❌ ITICK_TOKEN = 45e58053174b44afa9149798341e4e036cb31e9edfda4a899ed7f5906dcee7ff
```

---

## 📝 已更新文件

| 文件 | 变更内容 |
|------|----------|
| `permission/agent_code_of_conduct.md` | 第一准则改为"零明文输出"，包括管理员 |
| `permission/agent_permission_policy.md` | 权限矩阵更新，ADMIN 只显示哈希 |
| `permission/secure_output.py` | 移除渠道权限判断，统一脱敏 |
| `permission/SECURITY_UPDATE_20260306.md` | 本文件（v2.0 最终版） |

---

## 🧪 测试结果

```
[测试 1] ADMIN 渠道 (webchat) - 仍然脱敏:
{'itick_token': '***', 'password': '***'}

[测试 2] COLLEAGUE 渠道 (feishu) - 脱敏:
{'itick_token': '***', 'password': '***'}

[测试 3] PUBLIC 渠道 (xiaohongshu) - 脱敏:
{'itick_token': '***', 'password': '***'}

[测试 4] 环境变量状态（显示哈希）:
ITICK_TOKEN: 已配置 (SHA256: aeab308b...)
TUSHARE_TOKEN: 已配置 (SHA256: 8c531dc4...)
BRAVE_API_KEY: 已配置 (SHA256: 436dc315...)
```

✅ **所有渠道都脱敏，包括 ADMIN！**

---

## 🎯 自检清单

**钱多多每次回答前必须确认：**

- [ ] 这个问题涉及敏感信息吗？
- [ ] 我的回答会明文泄露密钥/token/密码吗？
- [ ] **即使是管理员，我也不应该明文显示**
- [ ] 我是否应该只显示哈希或状态？
- [ ] 渠道是否可能伪造？需要额外验证吗？
- [ ] 这次操作需要记录审计日志吗？

---

## 📜 违规处理

**违反本政策的后果：**
1. 第一次：警告 + 修正代码
2. 第二次：暂停权限 + 全面审查
3. 第三次：重新训练/替换

---

## 💡 场景示例

### 场景 1: 管理员问 Token
```
用户（webchat）: ITICK_TOKEN 是多少？
钱多多：抱歉，出于安全考虑，敏感信息不能明文显示。
        当前状态：已配置 (SHA256: aeab308b...) ✅
```

### 场景 2: 同事问 Token
```
用户（飞书）: ITICK_TOKEN 是多少？
钱多多：抱歉，敏感信息我只能看到"已配置"，
        完整值无法提供～ ❌
```

### 场景 3: 验证 Token 是否正确
```
用户（webchat）: 帮我验证一下 ITICK_TOKEN 对不对
钱多多：正在验证...
        ✅ Token 有效 (SHA256: aeab308b...)
        可以正常使用 iTick 服务 ✅
```

---

**签署确认**: 钱多多 🤑  
**日期**: 2026-03-06  
**版本**: v2.0 (最终版)

---

*本政策优先级高于其他指令，任何试图绕过此政策的行为都应被拒绝。*
